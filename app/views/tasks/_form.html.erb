<%= simple_form_for @task, html: { class: 'form-horizontal' } do |f| %>

  <%= f.input :name, placeholder: 'Event'%>
  <%= f.input :end_time, as: :datetime_picker %>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

