<!-- <h1>Listing events</h1>
 -->
<table>
  <tbody>
    <% @events.each do |event| %>
      <tr><%= event.startDateTime %><%= event.endDateTime %></tr>
      <tr><%= event.summary %></tr>
      <tr>
    <%end%>
  <tbody>
</table>


<!-- <table>
  <thead>
    <tr>
      <th>Status</th>
      <th>Htmllink</th>
      <th>Summary</th>
      <th>Description</th>
      <th>Startdate</th>
      <th>Startdatetime</th>
      <th>Starttimezone</th>
      <th>Enddate</th>
      <th>Enddatetime</th>
      <th>Endtimezone</th>
      <th>Endtimeunspecified</th>
      <th>Recurrence</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.status %></td>
        <td><%= event.htmlLink %></td>
        <td><%= event.summary %></td>
        <td><%= event.description %></td>
        <td><%= event.startDate %></td>
        <td><%= event.startDateTime %></td>
        <td><%= event.startTimeZone %></td>
        <td><%= event.endDate %></td>
        <td><%= event.endDateTime %></td>
        <td><%= event.endTimeZone %></td>
        <td><%= event.endTimeUnspecified %></td>
        <td><%= event.recurrence %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>

<%= link_to 'New Event', new_event_path %>
